# Nginx config for simtkus.com
# Place in /etc/nginx/sites-available/simtk and symlink to sites-enabled

server {
    listen 80;
    server_name simtkus.com www.simtkus.com;

    # Allow certbot to use the ACME challenge
    location /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
    }

    # Redirect all other traffic to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name simtkus.com www.simtkus.com;

    # Paths used by certbot; certbot will populate these
    ssl_certificate /etc/letsencrypt/live/simtkus.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/simtkus.com/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;

    access_log /var/log/nginx/simtk.access.log;
    error_log /var/log/nginx/simtk.error.log;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

